# =============================================================================
# .gitignore — Projet ALIMA DHIS2
#
# Règle absolue : aucun secret ni aucune donnée de santé ne doit entrer dans ce
# dépôt. Les entrées ci-dessous en sont la première ligne de défense — elles ne
# remplacent pas la vigilance avant chaque commit.
# =============================================================================

# --- Secrets et configuration réelle -----------------------------------------
.env
.env.*
!.env.example
*.env.local
dhis.conf
!dhis.conf.template
*.pem
*.key
*.p12
*.jks
credentials.json
service-account*.json

# --- Données de production ----------------------------------------------------
# Données de santé ALIMA — aucun dump ne transite par ce dépôt (CGA, art. 13)
*.sql
*.dump
*.backup
*.tar.gz
*.tgz
backups/
dumps/

# --- Certificats TLS ----------------------------------------------------------
letsencrypt/
certs/

# --- Docker -------------------------------------------------------------------
docker-compose.override.yml

# --- Système ------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# --- Outillage d'assistance -----------------------------------------------------
# Les fichiers liés à Claude Code ne sont pas versionnés : ils relèvent de
# l'outillage individuel, pas de la définition du projet. La documentation de
# référence est dans docs/.
CLAUDE.md
.claude/

# --- Éditeurs -------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# --- Journaux -----------------------------------------------------------------
*.log

# --- Divers -------------------------------------------------------------------
.dist/
node_modules/
